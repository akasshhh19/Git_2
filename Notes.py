#1 git init to initialise as git repository if not made yet

#2 can re-initialize git repository using git init

#3 rm -rg .git --> to delete the repository

#4 git add .  --> to add all files in the current directory, does not involve files in the parent directory
#5 git add --a --> add all the files of the repository to the staging area

#6 
